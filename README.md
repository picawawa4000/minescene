# MineScene: Minecraft level viewer

## Usage

If you're reading this, there should hopefully be a binary provided for macOS. If not, or if you're on Linux/WSL, open the project and run `swift build`. Also note that SwiftPM can't handle SwiftSDL's SSH links correctly as of writing this, so you may have to change those to HTTPS links manually.
